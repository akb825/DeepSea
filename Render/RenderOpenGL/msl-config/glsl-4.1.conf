target = glsl
version = 410
define = DS_ADJUST_CLIP(v)=((v)*vec4(1.0, _dsInternal.x, 1.0, 1.0))
header-line-frag = layout(origin_upper_left) in vec4 gl_FragCoord;
header-line = uniform vec4 _dsInternal;
